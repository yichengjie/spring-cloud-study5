server:
  port: 8080
spring:
  application:
    name: gateway-server
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        - id: client-a
          uri: lb://client-a
          predicates:
            - Path=/client-a/**
            - TimeBetween=上午9:00, 下午2:00
logging:
  level:
    reactor.ipc.netty: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8686/eureka
