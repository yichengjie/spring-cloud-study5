1. 行为参数化
    ```txt
    1.1 行为参数化，就是一个方法接受多个不同的行为作为参数，并在内部使用他们，完成不同行为的能力。
    1.2 行文参数化可以让代码更好适应不断变化的需求，减轻未来的工作量。
    1.3 传递代码，就是将新行为作为参数传递给方法。但是在java8之前这实现起来很啰嗦。
        为接口申明需要只用一次的实体类而造成的啰嗦代码，在java8之前可以使用匿名类来减少。
    1.4 Java Api包含很多可以用不同行为进行参数化的方法，包括排序、线程和GUI处理。
    ```